let api_body = {
    "fld_contactname_27412890": "biz_infor_policyholder_name",
    "fld_contactnumber_9873757": "biz_infor_phone_number"
}

for (let key in api_body) {
    let value = api_body[key]
    console.log(key, ':', value)
}

console.log('Chuyển CVTV xử lý\t')

let api_body = {
    "fld_contactname_27412890": "biz_infor_policyholder_name",
    "fld_contactnumber_9873757": "biz_infor_phone_number"
}

for (let key in api_body) {
    let value = api_body[key]
    console.log(key, ':', value)
}

console.log('Chuyển CVTV xử lý\t')

// git push --set-upstream origin master
// git push --set-upstream origin master
// git push --set-upstream origin master
// git push --set-upstream origin master
